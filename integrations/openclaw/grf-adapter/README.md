# Nollm GRF OpenClaw Adapter

Native OpenClaw plugin for explicit capture and bounded, model-decided geometry placement. It does not implement memory-provider, vector, embedding, global discovery, or deterministic semantic placement behavior.
