# Nollm GRF

Use `nollm_prepare_placement` to prepare bounded placement context, call the bundled `llm-task` with JSON-only PlacementDecision output, then use `nollm_apply_placement`. Defer when identity or candidate cells are insufficient.
