# Changelog

## 0.1.0-beta.1

- Re-activate the formal OpenClaw memory slot package.
- Register standard `memory_search` and `memory_get` tool factories.
- Bind memory scope from host tool-factory context.
- Keep packaged runtime and automatic writer migration as explicit beta blockers.
