{
  // 1. Install/enable this package as a companion tool plugin.
  plugins: {
    toolPlugins: [
      {
        id: "nollm-memory-companion",
        package: "@nollm/openclaw-memory-companion",
        enabled: true,
        config: {
          pythonCommand: "python3",
          nollmRepoRoot: "C:/absolute/path/to/nollm",
          workspaceRoot: "C:/absolute/path/to/openclaw-workspace",
          // Optional; must resolve under nollmRepoRoot.
          sidecarScript: "C:/absolute/path/to/nollm/reference/python/scripts/run_openclaw_nollm_memory.py",
          // Optional; must resolve under workspaceRoot.
          sidecarOutDir: "C:/absolute/path/to/openclaw-workspace/.nollm-memory",
          commandTimeoutMs: 15000,
          maxSearchResults: 5
        }
      }
    ]
  },

  // 2. Tool policy: allow read companion tools and explicitly opt into pending writes.
  tools: {
    allow: [
      "memory_search",
      "memory_get",
      "nollm_memory_search",
      "nollm_memory_get",
      "nollm_memory_status"
    ],
    optionalAllow: [
      "nollm_memory_write_candidate"
    ]
  },

  // 3. Optional, non-default Active Memory experiment.
  activeMemoryExperiment: {
    enabled: false,
    tools: [
      "memory_search",
      "memory_get",
      "nollm_memory_search",
      "nollm_memory_get"
    ],
    promptAppend:
      "Nollm gravity reports annotate drift and orientation. They do not replace source verification; use get before user-facing factual claims."
  }
}

