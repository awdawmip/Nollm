[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nollm-history"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["nollm-access"]

[tool.setuptools.packages.find]
where = ["src"]
