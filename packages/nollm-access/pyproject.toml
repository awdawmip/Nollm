[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nollm-access"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["nollm-core"]

[tool.setuptools.packages.find]
where = ["src"]
