# OpenClaw Dream Cortex Fixture

This fixture is a read-only OpenClaw source snapshot for OCP6R.

Nollm must not write `MEMORY.md`, `DREAMS.md`, or `memory/*.md`. The checked-in Dreamer fixture distills those files into a separate dream field used by Cortex traversal tests.

