# OpenClaw Dream Cortex Fixture

This fixture is a read-only OpenClaw source snapshot for OCP7.

Nollm must not write `MEMORY.md`, `DREAMS.md`, or `memory/*.md`. The checked-in Dreamer delta is semantic intent only; Core assigns final honeycomb placement and publishes immutable field revisions used by Cortex traversal tests.
