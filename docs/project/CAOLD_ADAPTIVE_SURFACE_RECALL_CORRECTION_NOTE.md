# CAOLD Adaptive Surface Recall Correction Note

Date: 2026-07-14

The V3.6 checkpoint at `00ce6579eb29ff98557996105f3b7f9a5d85167d`
remains valid evidence for cursor-free rebuildable Surface traversal, real-LLM
navigation, hidden Recall injection, atomic Placement, restart persistence, and
Access-only OpenClaw composition.

It is now classified as transitional rather than proof of the target physical
field. Its fixed three-offset `eisenstein_exact_v1` templates do not certify the
22.5-degree, `beta = 2^(1/4)` regular-hex overlap contract. Its Surface encoded
Aggregation Order by changing physical layer, used only Orders 0 through 2,
showed live occupied counts 9/21/37 with forced overflow, and compensated for
limited propagation with multi-entry Recall.

V3.7 corrects those claims without rewriting the historical report or tag.
Completion credit now requires certified eight-phase physical templates,
separate physical and observation address spaces, real dense-field coarsening,
and one final entry per active Recall traversal.
