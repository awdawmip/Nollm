# CAOLD Lens Causal True Junction Hierarchical Atlas Report

## Result

`CAOLD_LENS_CAUSAL_JUNCTION_GROWTH_IN_PROGRESS`

Input HEAD was `dd95606b36acae753ce137c5c449f6fa16a070b4` on branch
`codex/caold-lens-causal-true-junction-hierarchical-atlas`. The Rev1
production contracts and bounded synthetic conformance are implemented, but
the Provider-backed completion minimum and the real durable long-arm gate are
not closed. This report does not claim final validation.

## Causal Contract

Recall Lens v2 remains operation-local. Access validates exact immutable
Capture spans, Atlas fingerprints, path-to-leaf bindings, canonical Lens
ordering, and unique geometry relation groups before invoking Core. Equivalent
resolved Lenses now fail before Core; the Sculptor prompt directs additional
perspectives at the same supplied Locality to remain honestly unresolved.
Lens text, future questions, path IDs, candidate IDs, and relation groups are
not persisted.

Core receives geometry addresses only. `RelationGroupJunctionRequest` scores
all bounded relation groups, occupancy, contact, and deterministic stable-key
tie breaking without receiving semantic text. Contact and relation-group
changes alter the selected candidate in fixed tests.

## Hierarchical Atlas

The Atlas is derived from the existing bounded Surface. It exposes order-0 and
order-1 nodes and complete two-node paths, not a stable-key prefix and not a
semantic index. A fresh Windows measurement over 1,000 Atoms in 300 occupied
Cells completed in `300.227 ms`: 64 nodes, 32 paths, 32 candidates, maximum
path length 2. The final stable-key Cell was exposed and Core state bytes were
unchanged. This is below the 5-second task target.

## Provider Writer Reader Critic

The final usable run executed 30 real `meituan/LongCat-2.0` calls over ten
scenario classes using independent random OpenClaw sessions and UTF-8
`--message-file` inputs. All 30 Host processes succeeded.

| Check | Result |
|---|---:|
| Writer production-contract valid | 9/10 |
| Reader single-entry `complete_inject` | 10/10 |
| Critic exact raw JSON contract | 1/10 |
| Valid Writer unresolved Lenses | 14 |
| Provider process failures in final run | 0 |

The invalid Writer changed the exact Capture quote from `每周日凌晨两点` to
`每周凌晨两天`; production span validation rejected it. Nine Critic responses
were JSON objects inside Markdown fences and remain invalid raw contract
outputs. They were preserved and were not silently repaired. One Critic also
disagreed with the direct unrelated-branch observation.

The direct Reader/counterfactual fixtures selected one target entry in 10/10
cases and kept the forced unrelated entry at a distinct geometry in 10/10
cases. That is useful Lab evidence, but it does not replace the missing
Provider minimum of ten valid complete Writer facts, five multi-relation-group
cases, and five no-bounded-junction cases.

Two earlier harness attempts are retained in the same JSONL. One has 30 Host
argument failures. The other has 30 successful processes but invalid truncated
command-line input. The final run moved prompts to `--message-file`; each
record includes prompt and response byte counts and SHA-256 values.

Average final-run Provider latency was `112009.937 ms` for Writer,
`27353.157 ms` for Reader, and `58555.392 ms` for Critic. Prompt sizes were
6,232-6,257 bytes, 1,514-1,548 bytes, and 2,710-4,569 bytes respectively.

## Long Arms And Negative Control

The synthetic long-arm conformance passed with Tokyo, absolute-time, weather,
and unrelated arms of length 2. Three independent single-entry recalls reached
the same target Handle with paths of length 2 or 3, while the unrelated arm did
not reach the target. The 100-case synthetic Lens suite also passed with 100
exact spans, 90 compiled relation groups, ten honest unresolved deferrals, no
duplicate Statements, and no persistent Lens text.

These results are explicitly `provider_backed=false`. The required real Host
flow of Provider Writer, hierarchical Atlas, true Junction, durable Admission,
restart, three outer-arm recalls, and unrelated negative control was not
completed. Gate G therefore remains open.

## Verification

- Core and Access: `183 passed, 8 warnings`.
- Snapshot, Trace, and OpenClaw Python: `58 passed`.
- M0 governance: `45 passed`.
- Lab: `16 passed`.
- OpenClaw Node: `44 passed`.
- Synthetic Lens conformance and synthetic long-arm conformance: passed.
- Large Atlas targeted tests: `4 passed`.

The evidence file has 168 canonical LF-delimited JSON lines, 682,551 UTF-8
bytes, and SHA-256
`7a884b37a32777964382fb618e73a1cdcc997d18f5ad4bd59a8c5e3fb62188b0`.
It contains full Host stdout/stderr and raw Writer/Reader/Critic output; no raw
Provider result was reconstructed.

## Actual Progress And Limits

The conservative capability estimate is `CORE 96 / SNAPSHOT 50 / TRACE 40 /
ACCESS 94 / HISTORY 10 / AUDIT 10 / OPENCLAW 92 / LAB 90 / DISTRIBUTIONS 95`.
The actual task vector is `CORE +8 / SNAPSHOT 0 / TRACE 0 / ACCESS +9 /
HISTORY 0 / AUDIT 0 / OPENCLAW +4 / LAB +8 / DISTRIBUTIONS +5`. These are
planning estimates, not release claims.

Remaining blockers are the Gate F Provider minimum and the real Gate G durable
long-arm proof. Multi-cell footprints, multi-physical-layer Placement,
multi-chart growth, persistent Lens state, semantic indexes, multi-entry
Recall, Provider replacement, and formal release remain out of scope.
