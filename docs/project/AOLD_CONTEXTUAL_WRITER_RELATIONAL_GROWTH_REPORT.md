# AOLD Contextual Writer And Shared-Retrieval Growth Report

Date: 2026-07-21

Status: `CONTEXTUAL_WRITER_SHARED_RETRIEVAL_GROWTH_VALIDATED_AT_75383c6f3cc0358d8dcc430cd09c02252edb947b`

Input checkpoint: `c40f8bf6a5be6184e5e56236c96d2817fed4bee5`

Validated code and Evidence commit: `75383c6f3cc0358d8dcc430cd09c02252edb947b`

## Entry Identity Closure

The c40f8bf reliability fixes remain accepted, but its live one-cell conclusion
is withdrawn. Every Progressive Atlas region had reused `progressive-entry:0`,
and fast Recall reduced those region-local identities through a single-key
dictionary. The Provider therefore saw one surviving entry.

Rev4 binds every selectable ID to the Atlas fingerprint, region identity, and
canonical GeometryAddress. Page construction rejects duplicate selectable
identities, and Reader construction validates counts instead of silently
overwriting candidates. The corrected nine-Cell field exposed nine unique
selectable entries. The target-hidden causal run received eight after the
target Cell and all target previews were explicitly removed.

## Writer And Cartographer

Writer v2 separates absorption sources from chronological same-session
context. The nine batches used context counts `0,1,2,3,4,4,4,4,4`, with a
maximum of 537 context characters. Every batch used one Writer call and one
Cartographer turn. Context Capture IDs can support exact spans and resolved
references but cannot become source ownership or re-enter absorption state.

The Writer emits direct queries separately from broader entry queries. Exact
span slices, source/context separation, absolute-date basis, and normalized
reference provenance are validated before writes. Cartographer v2 resolves
only entry queries and seeks a plausible shared future retrieval neighborhood;
it does not require an existing region to contain the new answer. Duplicate
resolved entry Cells in one plan fail closed.

## Nine-Fact Live Result

The isolated workspace `nollm-aold-contextual-relational-growth-v6` produced:

- 9 durable Captures, 9 unique Statements, 9 active Atom/Handle/Cell bindings;
- 9 reopen-verified commits and zero wrong dates;
- 6 `new_local` related placements and 3 `independent_seed` placements;
- zero duplicate active entry IDs, bindings, or occupied Cells;
- Tokyo, meeting, and weather product selections from three distinct entries;
- T0 reached from each relation arm, with non-empty paths;
- target-hidden T0 reached through `lateral` after its Cell and previews were hidden;
- audit-retention and server-backup counterfactual entries did not reach T0;
- restart/NONE returned no injection.

The target-hidden query, unrelated counterfactuals, and restart Recall preserved
Core bytes exactly: before and after SHA-256 was
`93364db1524433b27e8bb62c62dbff49ef7e71b0e3c190e82efc81a608de4b02`.

During the first NONE attempt, explicit `capture_enabled=false` was incorrectly
interpreted as permission to run legacy Formation. One negative proposition was
placed. The Host removed its Core/Binding state through public Access `forget`,
which restored the exact nine-placement SHA above. Recall-only mode now blocks
legacy Formation when both Capture and absorption are explicitly disabled; a
fresh real NONE run and Node hook regression prove zero writes. The immutable,
unbound Statement Evidence from that diagnosed attempt remains only in the
external audit workspace and is not part of the nine accepted admissions.

## Performance

The nine real Capture samples had an observed nearest-rank p95 of 182.0972 ms
because one cold sample was 182.0972 ms. This observation is preserved, not
rewritten. The fixed 150-sample controlled Capture Gate measured p95 7.6748 ms
and max 20.5886 ms, satisfying the 100 ms p95 requirement. Provider latency is
reported separately and is not presented as local Core performance.

## Verification

- Core, Snapshot, Trace, and M0: `143 passed`.
- Access and OpenClaw Python: `179 passed`, 8 existing deprecation warnings.
- OpenClaw Node: `51 passed`.
- Rev3/Rev4 Evidence and Cartographer focused checks: `12 passed`.
- Git-blob Evidence verifier passed at `75383c6f3cc0358d8dcc430cd09c02252edb947b`.
- Rev3 frozen causal result remains 10/10 with zero unrelated false reach.
- Ownership manifest: `2039 tracked / 2039 rows / 0 unclassified`.
- Module boundaries: `0` production violations, `0` production cycles, and 7
  preserved migration records.

## Scope And Delivery

Actual task movement is:

```text
CORE 0% | SNAPSHOT 0% | TRACE 0% | ACCESS +8% |
HISTORY 0% | AUDIT 0% | OPENCLAW +5% |
LAB +7% | DISTRIBUTIONS +5%
```

No Core geometry, multi-cell atom, multi-entry Recall, persistent relation
type, query/fact map, Topic/Entity, vector, graph, embedding, Stitch, or
multi-Chart behavior was added. Validation used one Windows workstation and
LongCat-2.0; it is not a general model-quality or cross-platform claim.

The delivery is one complete-history external Git bundle named
`nollm_aold_contextual_writer_shared_retrieval_growth_20260721_<shorthead>.bundle`.
Its final path, final delivery HEAD, bundle verification, clean-clone result,
and SHA-256 are recorded in the delivery response after the documentation
commit is fixed.
