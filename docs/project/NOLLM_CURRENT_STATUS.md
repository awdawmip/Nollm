# Nollm Current Status

Date: 2026-07-14

Current task: `NOLLM_CAOLD_ADAPTIVE_SURFACE_RECALL_TASK_20260714.md` (`COMPLETED`).

V3.6 is the active adaptive multi-scale Surface route. The accepted input is the real geometric cluster checkpoint `a515ae778888ec76ec258ff51e53ee283501dd9b`; the validated implementation/test checkpoint before final reporting is `e91fee1e`. The final capability tag is `REAL_OPENCLAW_ADAPTIVE_SURFACE_RECALL_VALIDATED_AT_<final-head>` and is resolved from Git after the report-containing commit.

Core derives rebuildable Order 0/1/2 projections from canonical geometry and Coverage. Access selects Active Surface only from fixed structural budgets, projects bounded current Handle previews, validates model choices, and performs canonical multi-entry ring-1 Recall. OpenClaw 0.6.0 uses hidden real-model Surface traversal for Recall and Placement with no Cursor, explicit cluster anchor, Python semantic router, main-agent tool, or direct Core import.

The source workspace was copied to `nollm-caold-adaptive-surface-v1` after a verifiable backup. Statements, Handles, canonical Core state, Bridges, and old workspaces were preserved. The old cursor SHA-256 was `928c345014d12d57c0436a06409b737b5940b8ae70007334d1c11957960d6c21`; it was not copied and no live cursor remains.

Windows live validation used real `meituan/LongCat-2.0` agents. Forced-coarse R1 and R2 both began at Order 2 but selected different Alpha and office regions. R1 recovered risk review, release window, and Priya; R2 recovered visitor-desk and cafe facts. A post-restart related query repeated bounded Recall. Under restored production budgets, R3 recorded `completed_none` with no entry or injection. Real Formation and Surface Placement continued with a new Alpha `new_local` write.

All package, M0, Lab, Node, ownership, and boundary gates pass. The plugin remains enabled and loaded with empty diagnostics. Production budgets are Recall 32 cells/160 units and Placement 48 cells/240 units. See `CAOLD_ADAPTIVE_SURFACE_RECALL_REPORT.md` for exact evidence and limitations.

This capability does not claim cross-platform portability, global semantic quality, multi-plane selection, large-scale performance, Stitch, History/Audit completion, long-term stability, or product release readiness. The next candidate capability requires a separately authorized taskbook.
