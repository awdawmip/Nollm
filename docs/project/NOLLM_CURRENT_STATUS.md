# Nollm Current Status

Date: 2026-07-14

Current task: `NOLLM_CAOLD_ROTATED_PHYSICAL_FIELD_SINGLE_ENTRY_SURFACE_RECALL_TASK_20260714.md` (`IN_PROGRESS`).

V3.7 is the sole active route. Work begins from the verified complete-history
bundle and commit `00ce6579eb29ff98557996105f3b7f9a5d85167d`. V3.6 remains
historical evidence for cursor-free rebuildable Surface traversal and real host
integration, but its fixed three-offset profile, physical-layer/Order
conflation, forced-overflow live gate, and multi-entry Recall are not accepted
as target physical-geometry completion.

The mandatory active geometry is pointy-top Model T with delta theta 22.5
degrees, `beta = 2^(1/4)`, adjacent-layer density ratio `sqrt(2)`, and finer
cells at increasing physical layers. Physical Memory Layer and Aggregation
Order must use separate address domains. Surface Order 0 through 8 must be
rebuildable from every supported native physical layer in FieldScope.

Active OpenClaw Recall will select one final entry and rely on bounded Core
Coverage, Lateral, and Bridge propagation. Natural reachability from multiple
independent entries is diagnostic only. Production placement remains limited
to physical layer 0 in this task.

Gate 0 verified the input bundle, branch, tag, clean source tree, full clone,
and `git fsck --full`. The live adaptive workspace contains 9 Cells and 9 Atoms,
all at `eisenstein_exact_v1/default/layer 0`, 12 Statement files, 9 bindings,
and no Bridge. A verified preservation copy and OpenClaw configuration backup
exist at `memory/backups/caold-rotated-physical-field-gate0-20260714`.

Current corrected baselines are Core 75%, Snapshot 50%, Trace 40%, Access 80%,
History 10%, Audit 10%, OpenClaw 80%, Lab 75%, and Distributions 65%.
