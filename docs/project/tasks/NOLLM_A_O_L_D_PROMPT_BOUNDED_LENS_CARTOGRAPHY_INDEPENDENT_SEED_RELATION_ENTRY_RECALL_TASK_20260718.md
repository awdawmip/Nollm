# Nollm AOLD：Prompt 有界 Lens Cartography、独立种子与关系入口召回任务书

**任务文件名**：`NOLLM_A_O_L_D_PROMPT_BOUNDED_LENS_CARTOGRAPHY_INDEPENDENT_SEED_RELATION_ENTRY_RECALL_TASK_20260718.md`
**日期**：2026-07-18
**受影响模块**：`A=Access | O=OpenClaw | L=Lab | D=Distributions`
**任务性质**：V3.11 Rev3 的 LLM 负担、自由场生长和因果召回闭环；Core realized Junction保持工作基线
**输入 Bundle**：`nollm_caold_field_complete_atlas_realized_junction_causal_loop_20260718_f9f01f5.bundle`
**输入 Bundle SHA-256**：`a804e14167408a3fb81c79adad336aa69174d101325d4ff3349cf8a38fa0cdb6`
**输入分支**：`codex/caold-field-complete-atlas-realized-junction-causal-loop`
**输入 HEAD**：`f9f01f56906c5ce64df4a4702f61d954a8402fb8`
**输入 Tag**：无
**建议分支**：`codex/aold-prompt-bounded-lens-cartography-relation-entry-recall`
**主环境**：Windows 10/11、PowerShell、Node 24、真实 OpenClaw / LongCat-2.0
**交付**：大跨度单任务；内部 Gate；普通问题直接修复；所有真实进展 commit；clean tree；仓库外单一完整历史 Git Bundle
**插件状态**：保持安装并启用
**数据状态**：旧工作区全部保留；新建 Rev3 工作区
**能力边界**：layer0、one Statement/Atom/Cell；不做multi-cell、多物理层、Stitch、Topic/Entity或语义索引

---

# 0. 任务推进向量

```text
任务推进向量：
CORE 0% | SNAPSHOT 0% | TRACE 0% | ACCESS +15% |
HISTORY 0% | AUDIT 0% | OPENCLAW +10% |
LAB +15% | DISTRIBUTIONS +5%

主方向：
撤回Reader 10/10的直接目标自证；
将Capture-only Writer与Prompt-bounded Cartographer分离；
Atlas完整性改为progressive pages而非巨大单Prompt；
允许无resolved Lens的完整事实创建独立中性seed；
生产Recall退出stable-key前32；
从relation entry以非空路径验证Writer几何；
完成三方向Provider长分支。

范围变化：
Access新增progressive Atlas/Cartography和independent_seed合同；
OpenClaw Dream Sculptor拆为Writer+Cartographer后台角色；
Recall entry投影改为Atlas-based；
Core公共合同不变。
```

## 0.1 Gate向量复述

每个Gate开始：

```text
C0 | S0 | T0 | A+15 | H0 | U0 | O+10 | L+15 | D+5
```

每个Gate结束记录：

```text
prompt bytes；
Atlas coverage；
Cartographer turns；
independent seeds；
relation-entry paths；
hidden calls；
实际向量和范围偏差。
```

---

# 1. 输入审核事实

## 1.1 基础

```text
Bundle SHA：
a804e14167408a3fb81c79adad336aa69174d101325d4ff3349cf8a38fa0cdb6；

HEAD：
f9f01f56906c5ce64df4a4702f61d954a8402fb8；

branch：
codex/caold-field-complete-atlas-realized-junction-causal-loop；

working tree：
clean；

exact HEAD tag：
无；

Manifest：
1962 tracked / 1962 rows / 0 unclassified；

production violations/cycles：
0/0。
```

独立回归：

```text
Core/Snapshot/Trace/Access/Lab/OpenClaw Python：
267 passed，8 warnings。
```

## 1.2 应保留

```text
Atlas coverage certificate；
explicit overflow；
realized-only Junction；
Writer durable same-field apply；
relation groups；
10个Provider Writer；
forced relation entries可经两步lateral到target；
一个Provider东京arm；
V3.10 Capture/worker；
Lens不持久化；
single-entry。
```

## 1.3 P0

### P0-A：Provider Reader直接选target

10/10 case：

```text
selected_entry == Writer target Cell；
target path == []。
```

独立重建同10个case：

```text
20/20 relation-group forced entries均可到target；
path均为[lateral,lateral]。
```

因此几何关系存在，但Reader Gate没有验证它。

### P0-B：Atlas Prompt爆炸

独立测量：

```text
300 occupied Cells：
Atlas order0 / 300 regions；
Dream Prompt ≈400361 bytes；

1027 occupied Cells：
Atlas order3 / 481 regions；
Dream Prompt ≈768225 bytes。
```

这与Writer平均112秒一致，不能成为长期架构。

### P0-C：Recall stable-prefix

当前：

```text
bounded_physical_entries
→ occupied stable-key sort
→ first32。
```

40 Cell fixture：

```text
q=39目标不在Reader entries。
```

### P0-D：独立seed被禁止

非空字段：

```text
action in new/reuse/revision/expand
+
relation_groups empty
→ invalid plan。
```

这直接造成Provider time fact反复：

```text
non-deferred action requires a resolved Lens relation group。
```

### P0-E：dense semantic visibility

Atlas region最多3条representatives。

```text
字段几何完整
≠ LLM看见全部current Statements。
```

reuse/revision不能依赖隐藏在region中的事实。

### P1

```text
报告写1961，最终Manifest实际1962；
Provider long-arm只完成Tokyo arm；
time/weather/unrelated/restart未完成；
one-cell仍未充分证实。
```

---

# 2. 当前能力定性

记录：

```text
FIELD_COMPLETE_ATLAS_AND_REALIZED_JUNCTION_CHECKPOINT_AT_f9f01f5
```

建议实际推进：

```text
CORE +5 | ACCESS +8 | OPENCLAW +4 | LAB +6 | D +4
```

建议完成度：

| 模块 | 当前 |
|---|---:|
| CORE | 95 |
| SNAPSHOT | 50 |
| TRACE | 40 |
| ACCESS | 90 |
| HISTORY | 10 |
| AUDIT | 10 |
| OPENCLAW | 90 |
| LAB | 86 |
| DISTRIBUTIONS | 93 |

---

# 3. 开工依据

按顺序读取：

```text
FIRST_PRINCIPLES；
PROJECT_BOOK V3.1；
CORE_FUNCTION_PRIORITY V3.4；
V3.7；
V3.9；
V3.10；
V3.11 Rev3；
CURRENT_STATUS；
Ledger；
本任务；
A/O/L/D charters；
AGENTS。
```

---

# 4. `AGENTS.md`

至少加入：

```text
- f9f01f5 is a field-complete Atlas and realized-Junction checkpoint, not a relation-entry Reader proof.
- A causal Reader success requires a non-empty path from a relation entry; selecting the target Cell directly is a separate control.
- Do not use occupied stable-key prefixes as the production Recall universe.
- A complete Atlas must be Prompt-bounded through progressive full pages, not one huge serialized field.
- Writer sees Capture only and forms Statements plus future Recall Lenses.
- Cartographer sees Lenses and operation-local Atlas pages and maps them to geometry.
- An independent complete proposition may create a relation-neutral seed when no Lens resolves.
- Do not force unrelated facts to attach to existing Localities.
- Atlas representatives are navigation hints; exact reuse/revision requires bounded local detail.
- Common batch uses one Writer call and one Cartographer session; do not recreate a session for every Lens.
- No single Writer/Cartographer Prompt may exceed the active byte budget.
- Preserve Core realized-Junction code unchanged unless a real regression is found.
```

---

# 5. 全部模块任务前完成度

| 模块 | 生命周期 | 完成度 | 置信度 | 缺口 | 影响 |
|---|---|---:|---|---|---|
| CORE | CAPABILITY_VALIDATED | 95 | 高 | 本任务仅回归 | 否 |
| SNAPSHOT | IMPLEMENTED | 50 | 中高 | 增量 | 否 |
| TRACE | IMPLEMENTED | 40 | 中 | metrics | 否 |
| ACCESS | IMPLEMENTED | 90 | 中高 | 巨大Atlas、无seed、Recall前缀 | 是 |
| HISTORY | PROPOSED | 10 | 低 | 暂停 | 否 |
| AUDIT | PROPOSED | 10 | 低 | 暂停 | 否 |
| OPENCLAW | IMPLEMENTED | 90 | 中高 | Writer/Cartographer混合、Reader直达 | 是 |
| LAB | IMPLEMENTED | 86 | 中 | 因果Gate自证、长分支不全 | 是 |
| DISTRIBUTIONS | IMPLEMENTED | 93 | 中高 | Rev3 Wire/Policy | 是 |

---

# 6. 目标完成度

| 模块 | 前 | 目标 | 交付 |
|---|---:|---:|---|
| CORE |95|95|回归|
| SNAPSHOT |50|50|回归|
| TRACE |40|40|回归|
| ACCESS |90|97|Progressive Atlas、seed、relation Recall|
| HISTORY |10|10|无|
| AUDIT |10|10|无|
| OPENCLAW |90|96|Writer/Cartographer、Reader|
| LAB |86|97|relation-entry因果和Provider arms|
| DISTRIBUTIONS |93|97|Rev3 schema/budgets|

不写永久100%。

---

# 7. Gate 0：状态和负例

加入机器负例：

```text
Reader direct target path=[]不算causal；
40 Cell target outside first32；
300/1027 Prompt size；
independent fact in nonempty field；
dense region hidden current Statement。
```

更新：

```text
AGENTS；
ACTIVE_PROJECT；
STATUS；
Ledger；
Rev3 architecture；
checkpoint commit。
```

---

# 8. Gate A：Capture-only Writer

## 8.1 Wire

新增：

```text
nollm_openclaw_proposition_writer_v1
```

输出：

```text
Statements；
source_capture_ids；
future Recall Lenses；
exact basis spans；
no_memory/defer。
```

不包含：

```text
Atlas IDs；
relation groups；
action；
Handle；
coordinates。
```

## 8.2 Batch

```text
每batch common Writer calls=1；
Prompt大小与Field规模无关；
多Capture provenance；
原始Statement内容不因Atlas变化。
```

## 8.3 兼容

旧Dream Sculptor v2：

```text
历史读取/迁移；
退出active Provider output。
```

---

# 9. Gate B：Progressive Cartographer

## 9.1 完整page

实现：

```text
top-level选择满足region/byte预算的最细Order；
包含全部regions；
region下降包含全部children；
不sample。
```

## 9.2 Session

```text
一个batch一个Cartographer session；
批量处理全部Lenses；
max turns默认4；
每turn一个Prompt/response；
不为每Lens创建session。
```

## 9.3 Actions

```text
open_region；
resolve_lens；
request_local_detail；
independent_seed；
defer。
```

## 9.4 Budgets

最低Gate：

```text
max regions/page <=32；
max prompt bytes <=64KB；
300 Cell每Prompt<=64KB；
1027 Cell每Prompt<=64KB；
coverage=100%；
overflow诚实。
```

## 9.5 State

Cartography traversal：

```text
operation-local；
不持久化；
Atlas fingerprint绑定；
mutation后失效。
```

---

# 10. Gate C：独立种子

## 10.1 合同

完整Statement且：

```text
all Lenses unresolved
```

允许：

```text
placement_mode=independent_seed。
```

Access使用：

```text
relation-neutral expand frontier。
```

## 10.2 不允许

```text
因“短期/低价值”no_memory；
伪造relation；
挂到任意早期坐标；
Topic/Cluster ID。
```

## 10.3 测试

```text
空字段首事实；
非空字段独立事实；
后续事实从seed生长；
restart；
active radius；
zero duplicate。
```

---

# 11. Gate D：Local detail与revision

如果Cartographer选择一个occupied region且需要比较current facts：

```text
Access打开该region的bounded current Statement page；
同一Cartographer session做一次local resolver；
决定reuse/revision/new/defer。
```

要求：

```text
不把整个Field Statements发出；
不建立语义索引；
隐藏Statement可通过region local page看见；
revision一次确认；
additive不错误revision。
```

---

# 12. Gate E：生产Recall退出坐标前缀

## 12.1 新入口视图

使用Prompt-bounded complete Recall Atlas。

不得调用：

```text
bounded_physical_entries first32
```

作为活动正确性入口。

## 12.2 一次隐藏调用

优先：

```text
一个Reader Prompt展示<=32完整top regions及其support；
Reader选择一个support entry或NONE。
```

大字段需要下降时，Codex可：

```text
在同一主代理/Reader session调用Atlas工具；
或记录slow path；
不得每页创建新child session。
```

## 12.3 大场

```text
40 Cell q39目标；
300 Cell后半部目标；
1000 Atom dense hidden target。
```

相关region必须可见。

---

# 13. Gate F：Relation-entry因果Gate

对每个Provider case：

```text
Writer；
Cartographer；
Admission；
reopen；
target Cell从Reader候选删除；
target Statement preview删除；
Reader只看到resolved relation entries + unrelated entries；
Reader选一个entry；
target path非空；
每个resolved Lens另做强制单入口；
unrelated不达。
```

必须分别报告：

```text
direct_target_control；
relation_entry_reader；
forced_lens_entry_reach；
unrelated_counterfactual。
```

完成标准：

```text
10/10 Writer durable；
10/10 relation-entry Reader target reach；
所有target path非空；
所有resolved Lens至少一个entry reach；
0 unrelated false reach。
```

---

# 14. Gate G：Provider自由场生长

新工作区：

```text
nollm-caold-prompt-bounded-cartography-v4
```

正常聊天形成：

```text
T0 independent seed；
Tokyo arm>=2；
time arm>=2；
weather arm>=2；
unrelated arm>=2。
```

禁止：

```text
direct Core semantic seed；
脚本指定arm candidate；
强制坐标。
```

Recall：

```text
三个关系入口；
target Cell隐藏；
path>=2；
同一T0；
unrelated不达；
restart；
NONE。
```

如果one-cell失败：

```text
IN_PROGRESS；
提交证据；
下一任务再研究multi-cell。
```

---

# 15. Gate H：V3.10/V3.11回归

保持：

```text
Capture；
scope/turn幂等；
worker drain/retry；
provenance/partial；
revision；
Pending+admitted；
hidden calls预算；
Lens不持久化；
realized Junction；
one Atom/Cell；
single-entry。
```

---

# 16. 性能与调用Gate

```text
Writer common calls=1；
Cartographer session=1；
turns<=4；
每Prompt<=64KB；
300/1027 fields无巨大Prompt；
Capture不退化；
Recall common hidden calls<=1；
Core local时间保持V3.9数量级。
```

Provider秒数作为观察，不作永久SLA。

---

# 17. Evidence和报告

主报告：

```text
docs/project/AOLD_PROMPT_BOUNDED_CARTOGRAPHY_RELATION_ENTRY_RECALL_REPORT.md
```

Evidence：

```text
validation/aold_prompt_bounded_cartography_relation_recall_20260718.jsonl
validation/aold_prompt_bounded_cartography_relation_recall_summary_20260718.json
```

冻结：

```text
Writer raw；
Cartographer每turn raw；
page certificates；
target-hidden Reader；
forced Lens entries；
long arms；
line/bytes/SHA；
Manifest；
Git blob复算。
```

---

# 18. 完成状态

全部通过：

```text
PROMPT_BOUNDED_LENS_CARTOGRAPHY_RELATION_ENTRY_GROWTH_VALIDATED_AT_<HEAD>
```

否则：

```text
AOLD_PROMPT_BOUNDED_CARTOGRAPHY_IN_PROGRESS_AT_<HEAD>
```

无论完成与否：

```text
commit；
clean；
完整Bundle；
不补造。
```

---

# 19. 非目标

```text
Core Junction重写；
multi-cell；
多物理层；
Stitch；
多Chart；
持久Lens；
Topic/Entity；
query/fact entry map；
vector/graph/embedding；
multi-entry Recall；
Provider更换；
PB；
正式发布。
```

---

# 20. Git和Bundle

建议提交：

```text
checkpoint(aold): adopt prompt-bounded cartography rev3
feat(openclaw): split proposition writer from field cartographer
feat(access): expose progressive complete Atlas pages
feat(access): admit relation-neutral independent seeds
fix(openclaw): replace stable-prefix recall entries
test(lab): validate relation-entry writer reader causality
test(aold): grow provider-backed three-arm field
docs(aold): record prompt-bounded cartography capability
```

最终Bundle：

```text
nollm_aold_prompt_bounded_lens_cartography_relation_entry_recall_20260718_<shorthead>.bundle
```

最终回复：

```text
branch/HEAD；
Writer/Cartographer calls；
Prompt bytes；
Atlas coverage；
independent seeds；
relation-entry paths；
large-field Recall；
long arms；
one-cell结论；
tests/manifest/boundary；
actual vector/completion；
limitations；
Bundle/SHA。
```
