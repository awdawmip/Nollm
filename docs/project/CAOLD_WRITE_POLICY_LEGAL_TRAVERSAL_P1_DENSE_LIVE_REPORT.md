# CAOLD Write Policy, Legal Traversal, P1, And Dense Live Report

**Date**: 2026-07-16
**Input bundle**: `nollm_caold_broad_residue_safe_field_dense_locality_in_progress_20260715_2e779764.bundle`
**Input bundle SHA-256**: `2b4b51afa508d00e67e117475798b1ec83f99c2bfcb0a873c63f60eb527e7f35`
**Input HEAD**: `2e779764cde6fbb1ef228444edfd55bd157b2b52`
**Input tag**: `CAOLD_BROAD_RESIDUE_SAFE_FIELD_DENSE_LOCALITY_IN_PROGRESS_AT_2e779764cde6fbb1ef228444edfd55bd157b2b52`
**Validated implementation/evidence HEAD**: `d85bd5f6c85cf071dc959dbbd4318a0ad24151f9`
**Status**: `CAOLD_LEGAL_TRAVERSAL_P1_DENSE_LIVE_IN_PROGRESS`

## Gate Ledger

| Gate | State | Evidence |
|---|---|---|
| 0 authority, status, and manifest | passed | input bundle verified; taskbook and status activated; omitted tracked validation added to manifest |
| 1 storage and active write policy | passed | Core owns storage canonicality; Access owns active semantic write policy |
| 2 state-derived legal actions | passed | Surface and physical-entry pages expose and enforce one state-derived action list |
| 3 bounded correction and singleton policy | passed | two-attempt operation-local correction; mechanical singleton resolution; deterministic runners pass |
| 4 recovered P1 live closure | passed | V5 performed one valid Core write, canonical binding, restart Recall, exact visible answer, and zero Recall-side rewrite |
| 5 dense real Live locality | in progress | no dense theme was started; no live truncated locality or three-statement cross-fact Recall is claimed |
| 6 regression and delivery | passed for IN_PROGRESS | fixed regression matrix and machine boundaries pass; complete-history bundle receipt is external |

Expected and actual module delta are both `CORE +5 | SNAPSHOT 0 | TRACE 0 | ACCESS +5 | HISTORY 0 | AUDIT 0 | OPENCLAW +5 | LAB +5 | DISTRIBUTIONS +5`. Current module progress is Core 100%, Snapshot 50%, Trace 40%, Access 100%, History 10%, Audit 10%, OpenClaw 97%, Lab 100%, and Distributions 90%. These percentages describe the current bounded route, not permanent completion.

## Implemented Contracts

Core validates storage-domain canonicality only. Access policy `nollm_default_dream_layer0_safe_write_v1` accepts only the default profile/chart, layer 0, null phase, and the declared active radius. Existing coverage metadata remains a geometry proof input rather than a generic Core write veto.

`SurfaceTraversalPage` and `PhysicalEntryPage` expose `legal_actions`, and navigation validates against that same tuple. Root pages omit parent return, continuation exists only with a next page, hard-max pages omit coarsening, Order 0 opens physical entries, and physical pages expose only valid continuation, return, select, none, and defer actions.

OpenClaw retries only malformed JSON or invalid traversal decisions, reusing unchanged operation state for at most two corrections. Provider timeout is separate evidence. Eligible singleton physical universes resolve mechanically; multiple candidates still require one explicit model selection. Plugin contract version is `0.12.0`. A module-scope, turn-bounded Recall fence suppresses completion-hook Formation even when Gateway dispatch crosses plugin registration instances.

`validation/caold_surface_legal_action_validation.json` and `validation/caold_traversal_correction_validation.json` pass. The latter proves illegal navigation changes no workspace bytes before recovery. Broad, safe-field, dense Surface, dense single-entry Recall, and natural multi-entry observation runners also pass. Synthetic dense evidence still contains 1,000 atoms, 300 occupied cells, and 31 truncated Surface cells; this is not presented as real dense Live evidence.

## P1 Live Evidence

The preserved source inventory has 34 Statements, 16 Core placements, and 13 occupied cells. Taskbook candidate `dream:b93c2e97cba5e002236190b205742368f1008cca452abaca023b663619545745` is absent. V1 through V4 are preserved failed copies: completion-hook Formation rewrote the recalled fact because the initial run/session fences were cleared too early or remained local to one plugin registration instance. V4 proves the defect with a second `revision_current` write after successful Recall.

V5 is the accepted P1 attempt at `C:\Users\Administrator\.openclaw\memory\nollm-caold-write-policy-legal-traversal-p1-dense-live-v5`. Normal chat formed Statement `dream:82b8b33fc8c0cf13220db726e34f67fe781ddaa073d5033c7d739766e3e268d9`. Placement action was `revision_current` with `core_write_count=1`; Statement count changed from 34 to 35 while Core remained 16 placements and 13 occupied cells. The exact canonical HandleBinding replaced the prior current revision without duplicate Statement, Binding, or Atom identity.

After Gateway restart, new session `caold-legal-p1-v5-recall-20260716` selected exactly that Statement and visibly answered `CR-7159`. Evidence then recorded `formation/suppressed` with reason `same_run_satisfied_by_recall`; post-Recall inventory remained 35/16/13. V5 evidence has 18 JSONL records and SHA-256 `dfc6134d03f44c3532d4a4e9254278569da8529b3232f8ebf6524162f2cf4880`.

Provider/model were `meituan/LongCat-2.0`. P1 write total was 143,655 ms: Formation 19,496 ms and Placement subagent 137,450 ms, with overlap inside the total operation. Restart Recall total was 42,879 ms, including 37,948 ms Recall agent time. Geometry/Surface timing and correction counters remain in the JSONL evidence; the accepted attempt required no traversal correction. Provider latency is not classified as geometry latency.

R1 Alpha, R2 Office, and R3 NONE remain preserved from the input checkpoint and regressions. The plugin remains enabled and points to V5. Source and failed V1-V4 workspaces remain intact. OpenClaw reported only known legacy update-check/config-health migration warnings.

## Dense Live And Limits

No 12-24 Statement dense theme was started in this window. Therefore real dense Statement/Handle/Core counts are zero, live truncated locality is absent, and no new-session single-entry Recall across three real dense facts is claimed. Gate 5 remains `IN_PROGRESS`; deterministic dense fixtures do not substitute for this missing Provider-backed evidence.

The next candidate capability is a separately bounded continuation of dense real Live accumulation and three-fact Recall on the preserved V5 workspace. Stitch/Unstitch, multi-layer semantic Placement, persistent Surface cache, semantic routes, graph/vector/embedding, and PB validation remain outside this task.

## Verification

Pytest passed: Core 70, Snapshot 7, Trace 3, Access 87, OpenClaw Python 37, and M0 45, for 249 tests total. Access emitted 8 existing deprecation warnings. Node adapter tests passed 22/22 and `plugin:check` passed. All seven deterministic runners returned `passed=true`. Ownership manifest check/validation, production boundary checks, `git diff --check`, final clean-tree check, and bundle verification are delivery gates.

The legal IN_PROGRESS tag is created on the delivery commit with prefix `CAOLD_LEGAL_TRAVERSAL_P1_DENSE_LIVE_IN_PROGRESS_AT_`. The final bundle filename, exact tag target, verified heads, and SHA-256 are external delivery evidence because a commit cannot contain its own identity and an included report cannot contain the hash of the bundle that contains it.
