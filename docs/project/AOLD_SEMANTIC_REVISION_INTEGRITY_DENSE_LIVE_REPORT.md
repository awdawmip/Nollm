# AOLD Semantic Revision Integrity Dense Live Report

## Status

`AOLD_SEMANTIC_REVISION_DENSE_LIVE_IN_PROGRESS`

Input bundle: `nollm_caold_write_policy_legal_traversal_p1_dense_live_20260715_f61efd5.bundle`

Input SHA-256: `e1f9b2d70551266eb40c83213d5949065c6e128a9fdc5b78423b714721055084`

Input HEAD: `f61efd5dcb46ffc8c8c79a5ca0ae588562346f8d`

Branch: `codex/aold-semantic-revision-integrity-dense-live-closure`

Implementation checkpoint through Gate 5: `930f363cede7cd69bbf827fa497456f11e74318b`
Evidence/report checkpoint: `7f30ed82731ba76f5876b6cc563439a6045796c5`

The planned vector was strict Placement semantics, destructive revision confirmation, V6 P1 repair, Provider-backed dense formation, hidden-preview Recall, and frozen evidence. All functional gates were reached. Final status remains `IN_PROGRESS` because the latest real-model C5 contrast rerun drifted to a forbidden destructive action.

## Input Audit

The supplied V5 evidence had 24 lines, 51,111 bytes, and SHA-256 `95bd1ced05207b156cca82bed71ee4f9c3db5a732656da18635ce2052182aa7e`, differing from the earlier report's 18-line snapshot. V5 contained 38 files, 35 Statement files, and 24,304 bytes. The named V3.7 long-form authority was absent from the bundle; no substitute was invented.

V5 incorrectly merged two different subjects through `revision_current`:

- BX: `dream:91b6c843119549038dd2bbb0a40a8452f82edf5c158cac39c877c691f22280dc`, `Alpha项目 V3.9 发布校验码是 BX-3917。`
- CR: `dream:82b8b33fc8c0cf13220db726e34f67fe781ddaa073d5033c7d739766e3e268d9`, `用户要求记忆：CAOLD广域余量验收的发布校验码是 CR-7159。`

## Revision Contract

Placement now distinguishes `reuse`, `revision_current`, `new_local`, `expand_surface`, and `defer`. A destructive revision remains provisional until one strict real-LLM confirmation returns the exact Wire:

```text
nollm_openclaw_revision_confirmation_v1
```

Reject and timeout are zero-write; the rejected target is operation-local; only one redecision is allowed. OpenClaw has no direct Core import and stores no confirmation state. Plugin version is 0.13.0.

The accepted real-model contrast receipt passed C1-C5: reuse, confirmed revision, new_local, new_local, defer. A final fixed-command rerun first returned empty non-JSON output; its one allowed rerun passed C1-C4 but returned `revision_current` for C5. The failed receipt proves sandbox zero-write and is retained at `validation/caold_revision_semantic_contrast_regression_attempt.json`.

## V6 P1 Repair

V6 is a non-destructive copy at `C:\Users\Administrator\.openclaw\memory\nollm-caold-semantic-revision-dense-live-v6`. Repair used public Access/Core APIs only. BX remained at `(-13,9)` with local atom `dream:09adbf...775a1`. A real Host Placement put CR at `(15,0)` with its own statement identity. Replaying the exact successful Host receipt reproduced the same state.

After restart, independent new sessions selected only the intended fact:

- Alpha selected BX at `(-13,9)` and visibly answered `BX-3917`.
- CAOLD selected CR at `(15,0)` and visibly answered `CR-7159`.

Both current-V6 Recall checks were zero-write. V5 was not modified.

## Dense Live

Five normal main-chat turns described the Qinglan exhibition hall. No script injected Statements, no Cell address was supplied, and no placement was forced. The fourth turn was truthfully suppressed because Recall had already satisfied that run. The fifth turn formed the pending fourth-turn material plus new facts into eight separate Statements.

Final public state:

```text
48 Statement files
29 HandleBindings
29 Core atoms
12 Qinglan current Statements
1 Qinglan supporting Statement
Qinglan cells: (4,0), (5,0), (6,0)
dense cell (5,0): 10 atoms, preview 3, truncated=true, remaining_count=7
```

All eight final statements were real `meituan/LongCat-2.0` Formation outputs and real Provider-backed `new_local` placements. No destructive revision occurred.

## Hidden Recall

After Gateway restart, session `aold-v6-qinglan-hidden-recall-final-20260716` asked for the equipment-failure contact, backup-key box, and fire-inspection schedule. Surface showed the equipment-failure anchor; the key and fire-inspection facts were hidden beyond the three-item preview.

Recall resolved one physical entry `(5,0)` and selected exactly three current Statements:

- `dream:1df31810252c866dfe6c401de674f68bdb9c05dc5ca1568f67a53cd41615ecc9`
- `dream:cd94b3b83c096c0346c101368e8c700fab59257dcb704584fc0044ba14bed281`
- `dream:7fcaaa0579e1382d03af310f2847fb86422a7a6b04465946bee6160e1f393e2c`

The visible answer supplied Shen Zhou, `QL-27`, and Friday 16:00/Xu Chuan. Its SHA-256 is `3bc3703a8b7e843f9a9cd04d4b79986e5abd34b806fb58855de90c97b6f54cb5`. Formation was suppressed and the workspace SHA remained `63b3ba345dc9ef1eb0a86c5d5c2ac92f24e6309a93fb5848384fa3321ecd1a45`.

## Evidence And Tests

Frozen evidence: `validation/caold_semantic_revision_dense_live_20260716.jsonl`

Lines: 160

Size: 374,849 bytes

SHA-256: `56eda45669a85bc8a36ea8679c26920bc9e8d963346997beef6942901b570a21`

Freeze summary: `validation/caold_semantic_revision_dense_live_summary_20260716.json`. It records provider/model, workspace identities, BX/CR bindings, contrast outcomes, dense counts, final entry, selected Statements, visible-answer SHA, and regression receipt.

Regression results:

```text
Core 70 passed
Snapshot 7 passed
Trace 3 passed
Access 93 passed, 8 deprecation warnings
OpenClaw Python 39 passed
M0 45 passed
Lab 14 passed
Node 23 passed
plugin:check passed
surface legal-action, traversal correction, P1 read-only, and Dense state runners passed
canonical ownership Manifest and module boundary checks passed
latest real contrast rerun failed C5 while remaining zero-write
```

## Boundaries And Limitations

No semantic index, embedding, graph, Cursor, multi-entry Recall, Python semantic placement rule, forced Cell, persistent Surface cache, or hidden-reasoning persistence was added. Old workspaces and plugins outside this linked task plugin were not rewritten. The Gateway restart CLI timed out twice, but each subsequent health probe reported the restarted service connected and loaded at version 0.13.0.

Because the latest C5 real-model result violates the contrast contract, no completion tag is authorized. Delivery uses the taskbook's legal `IN_PROGRESS` tag and preserves all successful functional evidence plus the failed regression receipt.
