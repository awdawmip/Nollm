# Layered Honeycomb Memory Field

Nollm is a layered rotating honeycomb memory field.

Cards exist at every scale. Each layer is made of regular hexagonal cards, and deeper layers increase density without creating an absolute leaf layer.

The honeycomb architecture is protocol language for orientation and audit. The current runtime remains filesystem-first and deterministic; it does not implement geometry runtime behavior yet.

