# Scale Scan Not Tree Descent

Recall is Scale Scan, not Tree Descent.

Cortex does not search for a leaf node. It scans across scales, re-evaluates active anchor fields at each layer, and may shift laterally if another field becomes stronger.

The scan stops when sufficient scale is reached.

