# SQLite As Audit Projection

SQLite is Audit Projection, not Memory.

Nollm memory remains Markdown, YAML, JSON, and JSONL. SQLite, if used, may project the ledger and cards for audit or inspection, but it must be rebuildable and disposable.

SQLite must not become the project identity, source of truth, semantic index, or memory engine.

