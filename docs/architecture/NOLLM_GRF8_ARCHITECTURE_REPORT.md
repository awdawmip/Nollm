# GRF8 Architecture

GRF8 keeps evidence and source fallback in Core. File, Codex, and OpenClaw
fixtures translate host contracts only; they do not own Evidence truth. The
active path is source capture, explicit placement, explicit admission, bounded
recall, and retained original source text.
