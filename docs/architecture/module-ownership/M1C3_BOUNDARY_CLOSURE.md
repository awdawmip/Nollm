# M1C3 Boundary Closure

M1C3 preserves zero production violations and cycles. Active Core owns frozen
profile/kernel contracts, stable-key geometry order, canonical Anchor sets, and
registered ring-1 lateral semantics. Active Access owns the process-local
workspace coordinator and consistent Core/Binding/Evidence reads. Legacy GRF
remains a Lab parity oracle only; no active package imports it.
