# AOLD Runtime Truth Manual Chat Handoff

Date: 2026-07-12

The user OpenClaw instance remains installed and enabled with the Nollm
Formation plugin. Normal chat requires no Nollm command or visible Formation
tool. The main assistant response remains the only user-visible response; the
background Dream uses `deliver=false`.

For a manual observation, send ordinary chat through the existing OpenClaw
surface. Do not include test commands or Formation instructions. Confirm the
main response is usable without waiting for any background notification. The
plugin's diagnostic trace is disabled outside an explicitly controlled Lab
run; background transcripts remain nonpersistent.

The active StatementStore and Nollm workspace are user data. Manual testing
must not clear, reset, or replace either location. Disabled or uninstall
comparisons belong only in a separate temporary OpenClaw profile with its own
workspace and state database.

This handoff validates interaction and lifecycle behavior only. It does not
claim MemoryStatement semantic accuracy, Placement, Recall, or long-term
quality.
