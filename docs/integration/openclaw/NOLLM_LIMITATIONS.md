# Limitations

Live hook and `llm-task` smoke validation are complete. Placement quality is not yet claimed: development corpus execution is in progress and evaluation remains frozen until development completion. No graph, vector, embedding, semantic search, or global relation index is used.
