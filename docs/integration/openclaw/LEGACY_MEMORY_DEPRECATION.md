# Legacy Memory Deprecation

Status: MT1.

`MEMORY.md`, `memory/*.md`, and `DREAMS.md` are legacy source records for archive import. They are not active recall truth for the V5 memory takeover route.

MT1 does not modify OpenClaw runtime, disable legacy tools, or delete legacy files. It establishes the archive and provenance chain needed before later runtime takeover stages.

Forbidden in MT1:

- primary prompt memory source changes
- legacy fallback on recall failure
- writes to legacy source files
- vector, graph, embedding, or raw archive scan recall paths
- automatic confirmation of imported legacy text

OCP9/OCP10 companion work remains negative evidence: Active Memory tool inheritance cannot provide the final primary/Cortex isolation boundary.
