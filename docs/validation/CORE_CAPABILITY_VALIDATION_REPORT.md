# Core Capability Validation Report

The executable record is generated by
`lab/nollm-lab/m1/run_core_capability_validation.py` and is bound to:

```text
validated_code_commit = a1c0b676c794ec7ca2bf20408cdf897bf2793c09
validated_code_tree_digest = c1f1fc3b9e491f96ab858eaea6ac63ccec75666e43a725f2425f61928929a041
validation_schema = nollm_core_capability_validation_v2
```

The digest covers Core source/tests, Snapshot source, Trace source, the Lab
geometry generator/compiler/research definitions/parity runner, and the
validator itself. `--check` recomputes that digest and all 25 capabilities
without writing the repository. Pure documentation commits may follow the
validated code commit.

Validated dimensions include semantic-blind atoms, addressed handles, atomic
commands and canonical state, 9/9 compiled templates, direct/coverage/lateral/
bridge Recall, Trace parity and state exclusion, stdlib-only Core, and absence
of semantic/global lookup APIs.

This is a capability record, not a sealed or final declaration.
