# GRA1 Rotation-Scale Resonance Scope

GRA1 is a pure geometry validation asset for finite rotation-scale recurrence and phase-drift separation under production ParameterSet B.

## Authorized Window

- Parameter id: `B`
- Max layer: `16`
- Layer gaps: `(1, 2, 4, 8, 16)`
- Base layers: generated per gap as `0..16-gap`
- Phase samples: production `DEFAULT_PHASE_SAMPLES`
- Phase policies: production `LAYER_PHASE_POLICIES`
- Axial center stencil: `(0,0)`, `(1,0)`, `(0,1)`, `(1,-1)`, `(2,-1)`
- Observation count: `432`

## Claims

- Gap 8 and gap 16 are finite integer-scale, hex-orientation commensurate candidates under production B.
- Exact center-sublattice alignment is measured only through production chart-center mapping.
- Layer drift is measured as phase separation, not as a semantic or trust signal.
- Every classification is recomputable from recorded numeric fields.

## Non-Claims

GRA1 does not select, modify, replace, or deprecate ParameterSet B. It does not create parent/child edges, covers, traces, compactions, admissions, FieldSnapshots, RecallUniverse, DreamShards, memory hierarchy, compression permission, or recall authority.
