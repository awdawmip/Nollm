# GRF8 Performance

The sustained runner records operation counts, latency quantiles, Windows
resource samples, disk ledger size, identity collisions, orphan count, and
snapshot replay. Current optimizations retain source fallback and avoid global
object-pair maintenance.
