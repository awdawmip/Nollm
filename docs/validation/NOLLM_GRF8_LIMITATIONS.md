# GRF8 Limitations

GRF8 validates Windows fixtures and deterministic datasets. It does not claim
production SLA, external dataset generalization, Linux/macOS support, or live
online Host services. Codex and OpenClaw are lifecycle fixtures in this stage.
