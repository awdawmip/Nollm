# NOLLM GRF7 Global Directory Report

Gate A passes. The measured manifest contains 100 partitions, 100 directory
entries, 198 directed neighbor links, and 110,177 canonical bytes. Replay
produces identical canonical bytes and directory growth is monotonic.

Descriptors contain boundaries, counts, snapshot/ledger refs, neighbors, and
bridge summaries. Tests reject actual Evidence content and semantic-edge keys.
Adding or removing a descriptor does not mutate partition objects. Core has no
Host or Terminal import.

Raw: `experiments/grf/results/GRF7_GLOBAL_DIRECTORY_MANIFEST.json`.

`GATE_A_PASS`.
