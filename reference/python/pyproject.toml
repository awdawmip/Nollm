[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nollm-reference"
version = "0.1.0"
description = "Reference filesystem CLI for Nollm."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
nollm = "nollm.cli:main"

