# Invalidated Development Run

This run was invalidated before evaluation because the pre-fix runner included
the corpus `expected` field in the model prompt. Results are retained for
execution forensics only and must not be used for placement metrics.
