# GRF Mini Validation

This directory contains deterministic local GRF1-E fixtures and baselines.

The baseline section is Cognee-style local comparison only. It is not an
actual Cognee run and does not connect to a graph/vector service.

Run:

```powershell
python experiments/grf/run_mini_validation.py
```
