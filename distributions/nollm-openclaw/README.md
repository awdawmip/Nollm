# nollm-openclaw

Composition target: `nollm-minimal` plus a future Access-only OpenClaw adapter.
The current adapter is a migration asset; live activation remains paused and
requires a future OpenClaw E2E task.
